.task
bin
frontend/dist
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Stray binary outputs (e.g. cmd/eventprobe stops here if -o omitted)
/*.exe

# Claude Code local state
.claude/settings.local.json
.claude/cache/
.claude/sessions/
.claude/state/

# Planning artifacts stay out of the repo — they are process notes, not
# product, and captures taken while debugging can carry account details.
docs/plans/

# macOS / editor
.DS_Store
.vscode/
.idea/